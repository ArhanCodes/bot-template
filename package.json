{
  "name": "bot-template",
  "version": "1.0.0",
  "description": "A pure Quill Discord bot template with /ping, /ban, /kick, /timeout.",
  "private": true,
  "scripts": {
    "start": "quill run bot.quill",
    "build": "quill build bot.quill bot.js"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.5"
  }
}
